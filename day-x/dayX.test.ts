import { answer1, answer2 } from "./dayX";
const input = [];

test("uses the first method to transverse", () => {
  expect(answer1(input)).toBe(X);
});

test("uses the second method to transverse", () => {
  expect(answer2(input)).toBe(X);
});
